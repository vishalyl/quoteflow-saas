# Copy to .env and fill in. .env is gitignored.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# NOTE: the OpenAI key must NOT live here — anything prefixed VITE_ is inlined
# into the public JS bundle. It moves to a Supabase Edge Function secret.
